# RAG-Eval

RAG-Eval is a production-oriented Retrieval-Augmented Generation system focused on measurable retrieval and generation quality.

The repository is being implemented in phased, test-gated increments. See `docs/implementation-state.md` on development branches for phase status and verification evidence.
