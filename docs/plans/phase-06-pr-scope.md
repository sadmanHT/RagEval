# Phase 6 PR Scope

The Phase 6 pull request covers dense embedding providers, real local Qdrant collection/index lifecycle, filtered dense retrieval, deterministic integration evidence, and cumulative regression. It does not implement BM25, fusion, reranking, generation, or evaluation-runner functionality reserved for later phases.
