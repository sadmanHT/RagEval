# Phase 6 Hosted Embedding Reference

The hosted reference configuration is OpenAI `text-embedding-3-large` with an explicit 3072-dimensional default. The implementation does not claim embedding API fine-tuning. Provider calls remain behind the dense embedding protocol and live-provider validation is additive to deterministic/local acceptance.
